<%= render :partial => "/share/header" %>
<div id="forget-body">
  <div class="main">
    <ul class="show">
      <li class="show">
        <div class="show">
          <div class="show-username">
            <%= link_to(h(@reminder.user.login), :controller => :reminders, :user => @reminder.user.login) -%>
            <%= t(:user_reminders, :scope => :text, :user => "") %>
          </div>
          <div class="show-title">
            <h2><%= h(@reminder.title) -%></h2>
          </div>
          <%= render :partial => "/reminders/show_detail", :locals => {:reminder => @reminder} %>
          <table class="buttons">
            <tr>
            <% if copiable_reminder?(@reminder) %>
              <td><%= button_to t(:copy, :scope => [:controller, :reminders]), action_path(:copy, :controller => :reminders, :user => current_user.login, :id => @reminder.id) -%></td>
            <% end %>
            <% if current_user? %>
              <td><%= button_to t(:check, :scope => [:railties, :scaffold]), action_path(:check, :id => @reminder.id) if @reminder.today_remind? -%></td>
              <td><%= button_to t(:edit, :scope => [:railties, :scaffold]), action_path(:edit, :id => @reminder.id) -%></td>
            <% end %>
            </tr>
          </table>
          <div><%= link_to_back_list %></div>
        </div>
      </li>
    </ul>
  </div>
</div>
